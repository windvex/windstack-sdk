{
  "name": "windstack-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf packages/*/dist packages/*/*.tsbuildinfo",
    "build": "tsc -b packages/core packages/evm packages/solana packages/vexanium packages/session",
    "typecheck": "tsc -b packages/core packages/evm packages/solana packages/vexanium packages/session --pretty",
    "pack:dry-run": "npm pack --dry-run --workspaces",
    "validate": "npm run clean && npm run typecheck && npm run build && npm run pack:dry-run",
    "prepublishOnly": "npm run validate"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "author": "PT WIND KRIPTOGRAFI TEKNOLOGI",
  "description": "TypeScript SDK packages for Wind Stack, Wisp Wallet, Wind Explorer, and Vexanium dApps."
}
